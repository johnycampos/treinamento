/**
 * Rota /recurso
 * app.get('/',
 */

 /**
  * Métodos Http
  * Get busca uma informação do back-end - acessar uma informação no navegador
  *Post: criar uma informação no back-end - enviar uma informação para o backend
  *Put: alterar uma informação no back-end
  *DELETE: deletar uma informação no back-end
  */

  /**
   * Tipos de parametros
   * Query: parametros nomeados enviados na rota após "?" (filtros, paginação) se colocar o "&" pode ir adicionando mais de um filtro
   * Route Params: parametros utilizados para identificar recursos
   * Request: tudo que a aplicação recebe do navegador
   * Response: tudo que vai enviar para o navegador
   * Request Body: corpo da requisição, utilizado para criar ou alterar recursos
   */

   /**Bancos de dados
    *Sql: mysql entre outros
    *NoSql: MongoDB, CouchDB, etc
    *
    * Configurar o banco:
    * instalar o 
    * Driver: SELECT * FROM users (buscar todos usuarios do banco)
    * Query Builder: table('users').select('*').where()
    * 
    */

    /**pasta src(source) é tudo que voce cria
     * pasta database é onde fica o banco de dados
     */